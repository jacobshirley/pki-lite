load("//tools/bazel:defs.bzl", "devex_ts_module")

devex_ts_module(
    name = "pki-lite",
    exclude_srcs = glob([
        "src/test-fixtures/**",
    ]),
    test_also_in_src = True,
    deps = [
        ":node_modules",
    ],
)
